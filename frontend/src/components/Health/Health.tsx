import React from 'react';

const Health: React.FC = () => {
  return (
    <div style={{ display: 'flex', justifyContent: 'center', alignItems: 'center', height: '100vh' }}>
      <h1 style={{ fontSize: '48px', color: '#4caf50' }}>running</h1>
    </div>
  );
};

export default Health;